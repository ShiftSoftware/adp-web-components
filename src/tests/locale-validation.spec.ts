describe('Localization files', () => {
  it('English Localization', () => {
    expect(true).toBe(true);
  });

  it('Arabic Localization', () => {
    expect(false).toBe(false);
  });

  it('Kurdish Localization', () => {
    expect(false).toBe(false);
  });
});
